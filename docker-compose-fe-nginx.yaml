version: '3.8'

services:
  siba-fe-nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    container_name: siba_fe_nginx_container
    env_file: .env
    ports:
      - '${PORT}:8080'